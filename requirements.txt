# Core AI SDKs
anthropic>=0.40.0
openai>=1.0.0
google-genai>=0.8.0

# Environment variable loading from .env file
python-dotenv>=1.0.0

# GUI backend server (gui.html + server.py)
flask>=3.0.0
flask-cors>=4.0.0

# Filesystem event watching (used by autonomous_agent.py)
watchdog>=3.0.0

# Optional: compile AI-generated code into a standalone executable
# pip install pyinstaller
# pyinstaller>=6.0.0
